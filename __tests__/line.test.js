const {Line, Artboard} = require('../dist/laue.umd')
const {shadow, mount} = require('@vue/test-utils')

describe('Chart:Line', () => {
  describe(':props', () => {
    it(':color', () => {
      // Mount(Line, {
      //   provide: {
      //     Artboard
      //   }
      // })
    })
  })
})
